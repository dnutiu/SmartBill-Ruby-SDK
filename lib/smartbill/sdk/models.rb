# frozen_string_literal: true

module Smartbill
  module Sdk
    # Typed request/response models for the SmartBill Cloud REST API.
    #
    # Each model lives in its own file (e.g. `models/invoice.rb` defines
    # `Invoice`) and is autoloaded by Zeitwerk.
    module Models
    end
  end
end
